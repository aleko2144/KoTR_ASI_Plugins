[MOD]
Enabled = 1
DisplayConsole = 0

;оба параметра по умолчанию равны 1.0
;both variables is 1.0 by default

;вектор тяги/руления/торможения автомобиля?
;traction/steering/brakes vector?
vec1_coeff = 1.0

;вектор сопротивления вращению?
;roll resistance vector?
vec2_coeff = 0.15

;эти же переменные для тягачей и (полу)прицепов
;the same variables for tractors/semitrailers/trailers
vec1_roadtrain = 1.0
vec2_roadtrain = 0.6

;скорость в м/с когда внутриигровые величины становится равны vec1 и vec2
;speed in m/s when in-game values become equal to vec1 and vec2

;speed = 0.0       -> coeffs = 1.0
;speed = speed_min -> coeffs = vec1, vec2

speed_min  = 10.0